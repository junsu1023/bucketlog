# BucketLog 수동 테스트 체크리스트

> 지금까지 구현된 기능을 실제로 확인할 때 쓰는 목록입니다. `docs/MVP-SCOPE.md`의 기능 ID(G-xx, E-xx …) 기준으로 정리했습니다.
> □ 는 아직 안 해본 것, ✅ 는 이 세션 중 에뮬레이터로 실제 확인까지 한 것입니다.

**버그 수정 이력**
- [x] ✅ 목표 상세/온보딩 하단 액션 바가 시스템 내비게이션 바(3버튼 모드)에 가려 삭제 버튼 등을
      탭할 수 없던 문제 — `navigationBarsPadding()` 추가로 수정. 실기기(3버튼 모드)에서 삭제
      버튼이 시스템 바 위로 정상 노출되는 것 확인.
- [x] ✅ 반복형 목표의 "진행 기록" 다이얼로그에서 메모·사진이 둘 다 없으면 "이번에 한 걸로
      카운트할게요" 체크박스가 켜져 있어도 "기록하기" 버튼이 비활성화돼 저장이 불가능했던 문제
      (CLAUDE.md 규칙 3 "기록에 필수 입력을 만들지 마세요" 위반). UI의 `enabled` 조건과
      `AddProgressEntryUseCase`의 `require` 가드 둘 다에 `incrementCount`를 반영해 수정. 반복형
      목표에서 메모/사진 없이 카운트만으로 저장 → 카운트 정상 반영되는 것 실기기 확인.
- [x] ✅ E-04(기록 날짜 수정) 구현 중 발견 — `relativeDayLabel`이 미래 날짜를 처리하지 못해
      (`days <= 0`이면 무조건 "오늘"로 표시) 기록 날짜를 미래로 바꾸면 타임라인에 계속 "오늘"로
      잘못 표시되는 문제가 있었음. E-04는 애초에 "과거 날짜로 소급 기록"이 목적이라 날짜 선택기
      자체에서 미래 날짜를 고를 수 없게 막아(`SelectableDates`) 문제를 원천 차단함.

---

## 1. 목표 관리 (G)

- [ ] G-01 목표 생성 — 제목 + 유형(한 번/반복) + 카테고리
- [ ] G-02 목표 메모("왜 하고 싶은지") 입력 후 저장
- [ ] G-03 버킷 지정 — "2026년" / "언젠가" 전환
- [ ] G-04 반복형 목표에서 목표 횟수(targetCount) 입력
- [x] ✅ G-05 목표 수정 — 목표 상세 상단바 "수정" → 제목/메모/카테고리/유형/목표횟수/버킷연도 수정
      가능. 실기기에서 제목·카테고리 변경 후 저장 → 상세/홈 카드에 반영 확인. 시스템 뒤로가기로도
      상세 화면으로 정확히 돌아옴(수정 전엔 홈으로 튕기는 버그가 있었음, 같이 고침)
- [x] ✅ G-06 반복형이 목표치 도달 시 "완료 제안"이 뜨는지 (자동 완료 아님) — **이번 세션에 신규 구현**.
      `docs/MVP-SCOPE.md` §1.1에 P0로 명시돼 있었으나 코드에 없던 것을 발견해 추가함. 목표치 도달 시
      타임라인 위에 "목표 횟수를 채웠어요. / 완료해볼까요?" 배너 노출, 탭하면 기존 완료 다이얼로그로
      진입(자동完료 아님), 취소해도 진행 중 상태 유지되고 배너도 그대로 남는 것 실기기 확인
- [x] ✅ G-06 완료 처리(한 번 하기) — "완료했어요" → 회고 다이얼로그(E-08) → 확인 시 타임라인에 체크
      표시된 완료 노드 추가, 하단 액션 바가 "다시 꺼내기/삭제"로 전환됨을 실기기 확인
- [x] ✅ G-07 접어두기 — "이건 안 하기로 하기"(문구 확인: "포기" 없음, CLAUDE.md 규칙 2) → 이유
      입력 다이얼로그 → 확인 시 홈에서 즉시 사라지고 보관함 "접어둠" 탭(A-02)에 노출됨을 실기기 확인
- [x] ✅ G-08 되돌리기 — 완료 상태에서 "다시 꺼내기" → 타임라인의 완료 노드가 일반 진행 기록으로
      바뀌고 하단 액션 바가 진행 중 상태(퀵체크인+기록추가/완료/접어두기)로 복귀함을 실기기 확인
- [ ] G-09 마감일 — ⚠️ UI 미구현
- [ ] G-10 카테고리 7종 고정 세트 표시 확인
- [ ] G-11 정렬·필터 — ⚠️ 미구현
- [ ] G-12 연말 이월 — ⚠️ 미구현 (12월 되기 전이라 테스트 불가)

## 2. 기록 (E)

- [ ] E-01 퀵 체크인 — 홈 카드 / 상세 상단에서 한 줄 입력, 탭→입력→저장이 3초 이내인지
- [ ] E-02 진행 기록 — 메모 + 사진(0~5장) + 날짜
- [ ] E-03 사진 촬영(카메라 앱 호출) / 앨범 선택 둘 다
- [x] ✅ E-04 날짜 수정 — 과거 날짜로 소급 기록. **이번 세션에 신규 구현.** 저장소 계층엔
      `update()`가 이미 있었으나 UI가 전혀 없던 것을 발견해 추가. 타임라인 각 기록의 "수정"에서
      Material3 DatePicker로 날짜 변경 가능. 미래 날짜는 선택 자체가 막혀 있음(`SelectableDates`) —
      "소급 기록"이라는 스펙 취지에 맞춤. 과거 날짜로 바꾼 뒤 저장 → 타임라인 반영 실기기 확인
- [x] ✅ E-05 기록 수정/삭제 — **이번 세션에 신규 구현.** 완료 인증(COMPLETION) 제외, 체크인/진행
      기록에 "수정"(메모+날짜)/"삭제"(확인 다이얼로그, 되돌릴 수 없음 문구) 버튼 추가. 삭제 시
      반복형 카운트에 영향 없어야 하는 CHECK_IN 삭제 확인(카운트 그대로 유지), 크래시 없음 확인
- [ ] E-06 반복형 카운트 — 기록 시 +1 기본, 수동 조정
- [ ] E-07 완료 인증 — 사진 + 회고 + 완료일
- [x] ✅ E-08 회고 프롬프트 — 완료 다이얼로그에 공통 질문 4개 + 카테고리별(여행/도전/배움/관계)
      질문이 랜덤으로 뜨고, "다른 질문 보기"로 재추첨됨. 실기기에서 여러 번 눌러 질문이 바뀌는 것
      확인. 빈 칸으로 "완료했어요" 눌러도 저장되는 게 곧 "건너뛰기"라 별도 버튼은 안 만듦. 접어두기
      화면은 `docs/DESIGN.md` §3의 고정 질문("왜 마음이 바뀌었나요?") 그대로 유지 — 별도 뱅크 없음
- [ ] E-09 대표 사진 지정 — 미지정 시 최신 사진 자동 노출되는지만 확인(수동 지정 UI는 P1이라 없음)

## 3. 홈 (H)

- [x] ✅ H-01 진행 중 목표 카드 목록 — 실기기 확인
- [x] ✅ H-02 요약 헤더 "2026년 · N개 중 M개 완료" — 실기기 확인
- [x] ✅ H-03 카드에 대표 사진 + 제목 + 마지막 기록 시점 + (반복형) 진행률 — 실기기 확인(반복형
      M/N 표시는 반복형 목표로 별도 확인 필요)
- [x] ✅ H-04 카드 내 인라인 퀵 체크인 — 실기기 확인
- [x] ✅ H-05 2025 · 2026 · 언젠가 연도 전환 — `availableYears`가 실제 데이터에 있는 연도 +
      올해만 동적으로 보여주는 로직(`HomeViewModel`) 확인. 2025는 데이터 없으면 칩 자체가 안 뜸
- [x] ✅ H-06 빈 상태에서 프리셋 목표 제안이 뜨는지 — 실기기 확인(목표 접어둔 뒤 빈 상태 노출)
- [x] ✅ H-07 "작년 오늘" 배너 — 이번 세션에 신규 구현. 1년 전 기록이 있으면 우선 노출, 없으면
      1개월 전으로 대체(`pickThrowback` 유닛 테스트 4개). DB에서 `recorded_at`을 365일 전으로
      조작해 실기기에서 배너 노출 → 탭 → 해당 목표 상세로 이동까지 확인

## 4. 목표 상세 (D)

- [x] ✅ D-01 세로 타임라인 — 체크인/진행기록/완료가 각각 다른 크기·색의 점으로 시간순 표시,
      완료 노드는 체크 표시. 실기기에서 진행 기록 3장 사진 추가 → 타임라인에 즉시 반영 확인
- [x] ✅ D-02 퀵 체크인 필드 — 상세 화면 하단에 고정(스펙 표는 "상단"이라 적혀 있으나 실제로는
      액션 바와 함께 하단에 위치 — 스마트 넛지 딥링크로 들어왔을 때 포커스+키보드까지 뜨는 것도
      기존 세션에서 확인됨). 위치가 스펙과 다른 건 기존 설계 결정이라 이번엔 그대로 둠
- [x] ✅ D-03 액션 바 — 기록 추가/완료하기/접어두기(진행 중), 다시 꺼내기/삭제(완료·접어둠) 실기기 확인
- [x] ✅ D-04 진행 표시 — 한 번 하기 목표에서 "N개 기록"으로 누적 기록 수 표시 확인. 반복형
      M/N 표시는 반복형 목표로 별도 확인 필요
- [x] ✅ D-05 사진 전체화면 뷰어 — 이번 세션에 신규 구현. 타임라인 사진 탭 → 검정 배경 전체화면,
      "N / M" 카운터, 좌우 스와이프로 페이지 이동, 시스템 뒤로가기와 "닫기" 버튼 둘 다 정상 종료
      실기기 확인. 원본(썸네일 아닌 display) 해상도 경로를 쓰도록 `TimelineEntry`/`GoalDetailViewModel`
      변경

## 5. 보관함 (A)

- [x] ✅ A-01 완료한 것들 — 사진 중심 그리드. 실기기에서 완료 처리 → 그리드 반영 확인
- [x] ✅ A-02 접어둔 것들 — 별도 탭, 이유 표시. 실기기에서 접어두기 → 탭 반영 확인
- [x] ✅ A-03 전체 타임라인 — 이번 세션에 신규 구현. 목표 구분 없이 모든 기록을 시간순으로,
      행 탭 시 목표 상세로 이동(기존 "이번 달" 탭에 없던 것도 이 김에 같이 채움). 실기기 확인
- [x] ✅ A-04 간단 통계 — 이번 세션에 신규 구현. 완료한 목표 수 / 카테고리별 / 월별 완료 추이.
      완료 그리드가 이미 구독하던 데이터를 그대로 가공(새 쿼리 없음). 완료 0개일 때 빈 상태 문구,
      완료 1개 만든 뒤 통계 반영까지 실기기 확인

## 6. 온보딩 (O)

- [ ] O-01 프리셋 목표 카테고리별로 뜨는지, 탭하면 추가되는지
- [ ] O-02 온보딩 마지막에 "시작하기"로 첫 목표 등록 유도
- [x] ✅ O-03 알림 권한 요청 — 첫 목표 등록 **직후**(첫 실행 아님)에 뜨는지. 온보딩 프리셋 경로/AddGoal 직접 경로 둘 다 확인 완료. 실기기에서 시스템 권한 다이얼로그 승인 → `dumpsys package` 로 `POST_NOTIFICATIONS granted=true` 확인함

## 7. 알림 (N) — `docs/NOTIFICATIONS.md`

- [x] ✅ N-02 스마트 넛지 — 30일 이상 방치된 목표 중 가장 오래된 것 1개를 골라 알림. 탭 → 딥링크 → 목표 상세 퀵체크인 필드 포커스+키보드까지 실기기 확인. WorkManager 실제 OS 잡 등록(`dumpsys jobscheduler`)도 확인
- [x] ✅ N-01 월간 회고 — 이번 달 기록이 있으면 말일에 예약, 0건이면 예약 안 하고 기존 예약 취소.
      딥링크(`bucketlog://archive?month=yyyy-MM`) 탭 시 보관함 "이번 달" 탭으로 정확히 진입,
      해당 월 기록(메모+목표 제목+상대 날짜)이 뜨는 것까지 실기기 확인
- [x] ✅ N-03 목표별 리마인더 — 목표 수정 화면에서 리마인더 토글 on → 주1회/2주1회/월1회 칩
      노출(매일 옵션 없음) 확인. 저장 → DB에 reminder_interval/reminder_enabled 반영 확인.
      평가 로직(가장 오래 밀린 목표 하나 선정, 예산 소비 후 목표별리마인더 > 넛지 우선순위)은
      유닛 테스트로 검증(EvaluateNotificationsUseCaseTest, ScheduleGoalRemindersUseCaseTest) —
      실기기에서 실제 알림 발송까지는 미검증(N-02와 달리 이번엔 시간을 앞당겨 트리거하지 않음)
- [ ] N-04 마감 임박 — ⚠️ 미구현 (G-09 마감일 UI가 없어서 선행 조건도 없음)
- [ ] N-05 연말 회고 — ⚠️ 미구현
- [x] ✅ N-06 알림 설정 — 전체 on/off, 넛지 on/off, 알림 시각(9/12/18/20시) 선택 화면 확인. 예산 엔진(주 1회 상한, 21~09시 조용한 시간 연기, 유저 off 시 차단) 유닛 테스트 7개 통과(`NotificationBudgetTest`)

## 8. 기타 (M)

- [x] ✅ M-01 다크/라이트 — 시스템 따름(`isSystemInDarkTheme`) + 설정 화면에서 시스템/라이트/다크
      수동 전환 추가. 전환 시 앱 전체(홈/상세/설정)에 즉시 반영, 앱 재시작 후에도 선택 유지(영속화)
      실기기 확인. `SettingsStore`가 suspend 함수만 있고 Flow가 아니라서, 설정 화면(쓰기)과
      App() 루트(읽기)가 같은 `ThemeModeStore` 싱글턴을 공유하는 얇은 반응형 레이어를 추가함
- [x] ✅ M-02 백업/복원 — 아래 8번 항목 참고, 에뮬레이터에서 왕복 확인 완료
- [x] ✅ M-03 데이터 초기화 — 설정 화면 "데이터 관리" 섹션의 "전체 데이터 삭제" → 확인 다이얼로그
      (백업 먼저 권장 문구) → "삭제" → 결과 다이얼로그("모든 데이터를 삭제했어요.") 실기기 확인.
      삭제 후 홈이 "0개 중 0개 완료" 빈 상태로, 보관함 완료 탭도 빈 상태로 정상 전환됨을 확인.
      `run-as com.bucketlog find`로 사진 파일이 파일시스템에서도 실제로 제거된 것까지 확인.
      설정 화면에 섹션이 늘어나며 세로 스크롤이 없어 하단이 화면 밖으로 밀려나 있던 기존 버그도
      같이 고침(`verticalScroll` 추가)

## 9. 공유 (S)

- [x] ✅ S-01 완료 카드 이미지 저장/공유 — 사진+제목+완료일+회고를 합성한 카드를 시스템 공유
      시트로 전달. **구현 방식이 계획과 달라졌다**: 원래 Compose의 GraphicsLayer로 화면을
      캡처하려 했으나, 이 환경(에뮬레이터)에서 캡처된 이미지에 화면의 다른 내용이 섞여 들어가는
      재현 가능한 버그가 있었다(여러 가설을 검증했지만 다 원인이 아니었고, 최종적으로는 API 자체
      신뢰성 문제로 판단). Android `android.graphics.Canvas`/iOS Core Graphics로 각 플랫폼이
      직접 카드를 새로 그리는 방식(`platform/ShareCardRenderer.*.kt`)으로 재구현해 해결.
      실기기에서 사진 포함 카드 생성 → 시스템 공유 시트("Sharing image") → 실제 PNG 파일
      (1080x1920, 9:16) 정상 생성 확인. 사진 없는 완료 목표도 배경색만으로 깨지지 않고 렌더링됨
- [x] ✅ S-02 인스타 스토리 9:16 규격 — S-01과 별도 레이아웃 없이 카드 자체를 9:16(1080x1920)
      고정 해상도로 만들어 동시에 만족(`docs/MVP-SCOPE.md` "여유가 없다면 단일 레이아웃만이라도")

---

## 10. 백업/복원(M-02) 상세 체크리스트

이번 세션에서 구현. 이미 확인한 것과 아직 안 해본 것을 나눴습니다.

**확인 완료 (에뮬레이터 실기기)**
- [x] ✅ 설정 화면 "지금 백업하기" → SAF 저장 다이얼로그(`bucketlog-backup-YYYYMMDD.zip` 제안 파일명) → 저장 → "백업이 저장되었어요" 메시지
- [x] ✅ 저장된 zip을 macOS `unzip`으로 직접 열어봄 — `data.json`(goals/entries 메타), `version.txt` 정상. 표준 zip 도구로 읽힘(자체 구현한 STORED 포맷이 spec 호환)
- [x] ✅ 로컬에만 있는 목표를 하나 추가한 뒤 백업 파일로 복원 → 백업의 목표 + 로컬 전용 목표 **둘 다 남음** (비파괴적 머지 확인)
- [x] ✅ 복원 결과 메시지("목표 N개, 기록 N개를 불러왔어요") 정상 표시
- [x] ✅ `planRestore` 순수 함수 유닛 테스트 5개 통과 — id 충돌 시 백업이 이김 / 사진 파일 있을 때 포함 / 없을 때 Entry는 남고 사진만 빠짐 / 썸네일만 없어도 그 사진은 제외 / 여러 goal·entry 처리

**확인 완료 (추가)**
- [x] ✅ **사진이 포함된 기록으로 왕복 테스트.** 진행 기록에 사진 2장을 넣고 → 백업 → 전체 데이터
      삭제(M-03) → 복원 → 홈 카드/목표 상세 타임라인 양쪽에서 사진이 실제로 다시 보이는 것,
      `run-as` 로 원본+썸네일 파일이 실제로 디스크에 복구된 것까지 확인. 버그 없음.

- [x] ✅ 손상된 zip 파일(확장자만 zip인 텍스트 파일)로 복원 시도 → "백업 파일을 읽을 수 없어요" 에러
      메시지 정상 표시, 크래시 없음, 기존 데이터도 그대로 유지됨 확인
- [x] ✅ `schemaVersion`을 2로 손으로 바꿔 재압축한 백업으로 복원 시도 → "이 백업은 최신 버전의
      앱에서 만들어졌어요. 앱을 업데이트한 뒤 다시 시도해 주세요." 메시지 정상 표시, 크래시 없음

**아직 안 해본 것 — 우선순위 순**
- [ ] **iOS에서 백업/복원 왕복.** iOS의 `ZipArchiver`(직접 구현한 STORED zip)와 `UIDocumentPickerViewController` 기반 피커는 시뮬레이터/실기기에서 아직 한 번도 실행해보지 않았습니다 — 컴파일만 확인했습니다(`compileKotlinIosSimulatorArm64` 성공). `UIDocumentPickerViewController`의 생성자 파라미터 이름(`uRL`, `documentTypes`, `inMode`)은 추측해서 작성했고 컴파일은 통과했지만, **실제 기기에서 피커가 뜨는지 · 저장/열기가 실제로 동작하는지는 미검증**입니다.
- [ ] **Android → iOS 상호 호환.** Android에서 만든 백업 zip을 iOS 앱으로 가져와서 복원되는지 (같은 STORED 포맷이라 이론상 되어야 하지만 실제 확인은 안 함)
- [ ] 목표 50개·기록 300개 정도의 대용량 상태에서 백업/복원 속도 — `docs/MVP-SCOPE.md` §5 완료 기준에 있는 규모 테스트
- [ ] 백업 파일 여러 개를 연속으로 복원했을 때 누적 동작(멱등성) — 같은 백업을 두 번 복원해도 중복 안 생기는지
- [ ] 알림 권한 다이얼로그(POST_NOTIFICATIONS)가 뜬 상태에서 백업/복원 버튼을 눌러도 문제없는지 같은 사소한 동시성 케이스는 낮은 우선순위

---

## 11. 자동화된 테스트 현황

`./gradlew :shared:testAndroidHostTest` 로 실행되는 것들:

| 파일 | 테스트 수 | 대상 |
|---|---|---|
| `PickNudgeTargetUseCaseTest` | 7 | N-02 스마트 넛지 대상 선정 로직 |
| `NotificationBudgetTest` | 7 | 알림 예산(주 1회 상한, 조용한 시간, on/off) |
| `PlanRestoreTest` | 5 | M-02 복원 머지 판단 로직 |
| `ScheduleGoalRemindersUseCaseTest` | 8 | N-03 목표별 리마인더 대상 선정·발송 시각 갱신 |
| `EvaluateNotificationsUseCaseTest` | 4 | N-01/N-03/N-02 우선순위 중재(월간회고 > 목표별리마인더 > 넛지) |
| `ThrowbackBannerTest` | 4 | H-07 작년 오늘 배너 선택 로직(1년전 우선, 없으면 1개월전 대체) |

`ZipArchiver`(Android/iOS 각각의 실제 zip 압축/해제)와 `Export/RestoreBackupUseCase`는 플랫폼 I/O(파일, DB)에 의존해 순수 유닛 테스트로 못 덮었습니다 — 위 10번 항목의 실기기 확인이 이 부분의 유일한 검증입니다.

---

## 참고: 이 문서 사용법

- 체크박스는 사람이 직접 눌러보고 확인한 뒤 체크하는 용도입니다. 코드 존재 여부만으로 자동 체크하지 않았습니다.
- "⚠️ 미구현"이라고 적힌 항목은 테스트할 대상 자체가 없으니 건너뛰어도 됩니다 — 구현 누락 여부를 한눈에 보려고 일부러 남겨뒀습니다.
- 새 기능을 추가할 때마다 이 파일에 체크리스트를 이어서 추가해주세요.
