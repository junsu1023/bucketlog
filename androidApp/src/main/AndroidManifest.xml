<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 알림(5주차): API 33+ 런타임 권한. docs/NOTIFICATIONS.md §4 — 요청 시점은 첫 목표 등록 직후. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".BucketLogApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:launchMode="singleTop"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 알림(5주차) 딥링크. docs/ARCHITECTURE.md §6 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="bucketlog" />
            </intent-filter>
        </activity>

        <!-- 홈 화면 위젯(Phase 1) "오늘의 기억" — docs/ROADMAP.md -->
        <receiver
            android:name=".widget.ThrowbackWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_throwback_description">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/throwback_widget_info" />
        </receiver>

        <!-- 홈 화면 위젯(Phase 1) "지금 어때요" — docs/ROADMAP.md -->
        <receiver
            android:name=".widget.NudgeWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_nudge_description">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/nudge_widget_info" />
        </receiver>

        <!-- 홈 화면 위젯(Phase 1) "마감 임박" — docs/ROADMAP.md -->
        <receiver
            android:name=".widget.DueSoonWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_due_soon_description">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/due_soon_widget_info" />
        </receiver>

        <!-- 카메라 촬영 결과를 임시로 담는 파일을 시스템 카메라 앱과 공유하기 위한 FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>